# ---------------------------------------------------------------------------
# 小程序端构建期变量模板 —— 复制为 .env.local 后填写（.env.local 已被忽略）
# ---------------------------------------------------------------------------

# 后端地址。
#   · 微信开发者工具「不校验合法域名」时：可填 http://127.0.0.1:3000 或 http://192.168.x.x:3000
#   · 真机预览 / 体验版 / 正式版：必须是 https + 已备案域名，且在公众平台
#     【开发 → 开发管理 → 服务器域名】里加入 request 合法域名
VITE_API_BASE=http://127.0.0.1:3000

# 仅 H5 本地联调用（模拟 AppID → 租户）。小程序端由微信注入真实 AppID，不需要填
VITE_APPID=
