# 接口基址。
#
# 留空（默认）= 同源相对路径 —— 生产由 Nginx 把 /api 与 /t 反代到服务端。
# 这样产物里不会写死任何域名，换环境只改 Nginx，不需要重新构建。
# 只有在"网页与服务端不同域"时才需要填（例如本地联调且没走 Vite proxy）。
VITE_API_BASE=

# 部署基址。**与构建期的路由 base 是同一个值**，改动必须两边一起改：
#   独立域名 / 根目录  → 留空（默认 /）
#   挂在子路径 /admin/ → VITE_BASE=/admin/，构建命令：
#                        VITE_BASE=/admin/ npm run build:web
# 两边不一致的表现：静态资源能加载但刷新子页面 404（或反过来）。
VITE_BASE=
